# Server Configuration
NODE_ENV=development
PORT=3001

# Server URL (for config endpoint response)
# In production, set to your Railway URL
SERVER_URL=http://localhost:3001

# CORS - Your Next.js app URL
# Local: http://localhost:3000
# Production: https://your-app.vercel.app
CLIENT_URL=http://localhost:3000

# Redis (Railway auto-injects REDIS_URL)
REDIS_URL=redis://localhost:6379

# Socket Authentication Secret
# Must match main app's CHAT_SERVER_SECRET or JWT_SECRET
# In production, set a strong random secret
CHAT_SERVER_SECRET=your-chat-server-secret

# API Authentication (for /api/config endpoint)
# These credentials are used by the main app to get server config
CHAT_API_KEY=chat-api-dev-key
CHAT_API_SECRET=chat-api-dev-secret
